#include <stdio.h>

typedef struct node TNode;
struct node{
	int father;
	int level;
	int isleaf;
};


int main()
{
	int i;
	int N, M;
	int id, K;
	TNode tree[100];
	scanf("%d%d", &N, &M);
	for( i = 0; i < N;)
	for( i = 0; i < M; i++ )
	{
		scanf
	}
}